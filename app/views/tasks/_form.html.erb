<%= form_for(@task) do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title %>
  <%= f.label :details %>
  <%= f.text_field :details %><br><br>
  <% unless @task.new_record? %>
    <%= f.check_box :completed %> Completed
  <% end %>
  <br><br>
  <%= f.submit %>
<% end %>
